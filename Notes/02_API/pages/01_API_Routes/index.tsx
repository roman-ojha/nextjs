import React from "react";

/*
    *) API Routes:
        -> first we have to create 'api' folder inside the 'page' folder
        -> we can create a file with any name which will automatically map to '/api' and treated as api endpoint instead of an page
        -> we will create the first api route inside '../api/01_API_Routes'
        -> the code that we write in api folder will never bundle with the next js frontend code
*/

const Index = (): JSX.Element => {
  return <></>;
};

export default Index;
