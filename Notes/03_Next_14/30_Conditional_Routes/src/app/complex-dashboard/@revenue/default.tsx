import Card from "@/components/card";
import type { NextPage } from "next";

const DefaultRevenueMetrics: NextPage = () => {
  return <Card>Default Revenue Matrix</Card>;
};

export default DefaultRevenueMetrics;
