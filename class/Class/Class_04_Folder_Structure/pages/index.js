/*
    => pages:
        -> api (for server side rendering)
        -> _app.js (entry point, defined main application layout)
        -> index.js (main file)
    => public: (for the public resources)
    => styles:
        -> globals.css (global css file for app)
        -> home.module.css (for individual css file)
    => .eslintrc.json: (this will highlight the program if there is error)
    => .gitignore
    => next.config.js:
    => package.json
*/
