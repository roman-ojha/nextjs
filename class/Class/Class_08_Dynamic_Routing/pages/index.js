/*
  => for dynamic route we have touse a special file naming convention that is to use [] (square brackets)
  => we have done that inside the '/blog' folder
*/

const index = () => {
  return (
    <>
      <h1>Hello world</h1>
    </>
  );
};

export default index;
