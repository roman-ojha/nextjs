/*
    -> first install next app globally:
        -> npm i -g create-next-app
    -> now create env:
        -> npx create-next-app <project_name>
    
    -> yarn start (to build app in production mode)
    -> yarn dev (to start the development server)
    -> 
*/
const index = () => {
  return (
    <>
      <h1>Hello world</h1>
    </>
  );
};

export default index;
